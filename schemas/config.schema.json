{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://legalize.github.io/paranimator/schemas/config.schema.json",
  "title": "ParAnimator configuration",
  "description": "Describes one animation render using an Id source par entry, output library files, and animated parameter tracks.",
  "type": "object",
  "required": [ "parameter-catalogs", "output", "video", "num-frames" ],
  "additionalProperties": false,
  "oneOf": [
    {
      "description": "Single-source animation form with top-level source and tracks.",
      "required": [ "source", "tracks" ],
      "not": { "required": [ "layers" ] }
    },
    {
      "description": "Layered animation form with one or more Id render layers.",
      "required": [ "layers" ],
      "not": {
        "anyOf": [
          { "required": [ "source" ] },
          { "required": [ "tracks" ] }
        ]
      }
    }
  ],
  "properties": {
    "parameter-catalogs": {
      "description": "Parameter catalog JSON files used to validate and resolve track targets.",
      "type": "array",
      "minItems": 1,
      "items": {
        "description": "Catalog filename or path read by ParAnimator.",
        "type": "string"
      }
    },
    "source": {
      "description": "Source Id par entry used as the base frame state.",
      "type": "object",
      "required": [ "file", "name" ],
      "additionalProperties": false,
      "properties": {
        "file": {
          "description": "Source par filename.",
          "type": "string"
        },
        "name": {
          "description": "Source entry name inside the source par file.",
          "type": "string"
        }
      }
    },
    "output": {
      "description": "Generated output locations and names for Id library files and batch script.",
      "type": "object",
      "required": [ "directory", "par", "entry", "script" ],
      "additionalProperties": false,
      "properties": {
        "directory": {
          "description": "Output directory containing generated par, map, image, and script files.",
          "type": "string"
        },
        "par": {
          "description": "Generated par filename written under the output par directory.",
          "type": "string"
        },
        "entry": {
          "description": "Generated frame entry name pattern.",
          "type": "string"
        },
        "script": {
          "description": "Generated batch script filename.",
          "type": "string"
        },
        "frames": {
          "description": "Final composed frame image filename pattern.",
          "type": "string"
        },
        "layers": {
          "description": "Rendered layer image filename pattern.",
          "type": "string"
        },
        "compose-script": {
          "description": "Generated ImageMagick composition batch script filename.",
          "type": "string"
        },
        "background": {
          "description": "Optional background color used to flatten final frames.",
          "type": "string"
        }
      }
    },
    "parallel": {
      "description": "Optional number of Id render commands to run in parallel.",
      "type": "integer"
    },
    "video": {
      "description": "Id video mode used for generated render commands.",
      "type": "string"
    },
    "num-frames": {
      "description": "Number of animation frames to generate.",
      "type": "integer"
    },
    "tracks": {
      "description": "Animated parameter tracks applied to the source par entry.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/track"
      }
    },
    "layers": {
      "description": "Layer stack evaluated from bottom to top.",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/layer"
      }
    }
  },
  "$defs": {
    "track": {
      "description": "One animated parameter target and its value source.",
      "oneOf": [
        { "$ref": "#/$defs/value-track" },
        { "$ref": "#/$defs/pwm-track" },
        { "$ref": "#/$defs/color-map-track" },
        { "$ref": "#/$defs/camera2d-track" },
        { "$ref": "#/$defs/id-3d-view-track" },
        { "$ref": "#/$defs/julibrot-view-track" }
      ]
    },
    "layer": {
      "description": "One Id image layer with its own source par entry and scoped tracks.",
      "type": "object",
      "required": [ "id", "source", "tracks" ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "Stable layer identifier used for naming and validation.",
          "type": "string"
        },
        "source": {
          "description": "Source Id par entry used as this layer's base frame state.",
          "type": "object",
          "required": [ "file", "name" ],
          "additionalProperties": false,
          "properties": {
            "file": {
              "description": "Source par filename for this layer.",
              "type": "string"
            },
            "name": {
              "description": "Source entry name inside the layer source par file.",
              "type": "string"
            }
          }
        },
        "opacity": {
          "description": "Keyed percent opacity for this layer from 0 through 100.",
          "$ref": "#/$defs/percent-number-track"
        },
        "write-when-hidden": {
          "description": "Render this layer even when evaluated opacity is 0.",
          "type": "boolean"
        },
        "compose": {
          "description": "Backend-neutral operator for this layer over the accumulated image.",
          "$ref": "#/$defs/compose-operator"
        },
        "tracks": {
          "description": "Animated parameter tracks applied only to this layer.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/track"
          }
        }
      }
    },
    "compose-operator": {
      "description": "Backend-neutral layer composition operator.",
      "type": "string",
      "oneOf": [
        {
          "const": "clear",
          "description": "Output transparent black."
        },
        {
          "const": "copy",
          "description": "Replace destination with source."
        },
        {
          "const": "destination",
          "description": "Keep destination unchanged."
        },
        {
          "const": "source-over",
          "description": "Place source over destination."
        },
        {
          "const": "destination-over",
          "description": "Place destination over source."
        },
        {
          "const": "source-in",
          "description": "Keep source only where destination alpha exists."
        },
        {
          "const": "destination-in",
          "description": "Keep destination only where source alpha exists."
        },
        {
          "const": "source-out",
          "description": "Keep source only where destination alpha is absent."
        },
        {
          "const": "destination-out",
          "description": "Keep destination only where source alpha is absent."
        },
        {
          "const": "source-atop",
          "description": "Place source atop destination while keeping destination alpha."
        },
        {
          "const": "destination-atop",
          "description": "Place destination atop source while keeping source alpha."
        },
        {
          "const": "xor",
          "description": "Keep source and destination outside their overlap."
        },
        {
          "const": "add",
          "description": "Add source and destination channels."
        },
        {
          "const": "subtract",
          "description": "Subtract source channels from destination channels."
        },
        {
          "const": "multiply",
          "description": "Multiply normalized source and destination channels."
        },
        {
          "const": "divide",
          "description": "Divide destination channels by source channels."
        },
        {
          "const": "min",
          "description": "Keep the smaller source or destination channel value."
        },
        {
          "const": "max",
          "description": "Keep the larger source or destination channel value."
        },
        {
          "const": "difference",
          "description": "Use absolute source and destination channel difference."
        },
        {
          "const": "average",
          "description": "Average source and destination channels."
        },
        {
          "const": "screen",
          "description": "Apply inverse multiply for glow and light layers."
        },
        {
          "const": "overlay",
          "description": "Multiply dark areas and screen light areas."
        }
      ]
    },
    "track-kind": {
      "description": "Application-level kind of animation track.",
      "type": "string",
      "oneOf": [
        {
          "const": "color-map",
          "description": "Track that writes generated Id map files and emits colors=@filename."
        },
        {
          "const": "id-3d-view",
          "description": "Virtual track that writes Id Euler-style 3D view parameters."
        },
        {
          "const": "julibrot-view",
          "description": "Virtual track that writes Id Julibrot slice and stereo parameters."
        }
      ]
    },
    "track-format": {
      "description": "Output value format used by a virtual track.",
      "type": "string",
      "oneOf": [
        {
          "const": "at-file",
          "description": "Emit an Id at-file reference such as @colors-0001.map."
        }
      ]
    },
    "track-mode": {
      "description": "Alternate evaluation mode for a track.",
      "type": "string",
      "oneOf": [
        {
          "const": "pwm",
          "description": "Pulse-width modulation between two explicit discrete values."
        }
      ]
    },
    "value-track": {
      "description": "Parameter track defined by keyed values or a path generator.",
      "type": "object",
      "required": [ "parameter" ],
      "additionalProperties": false,
      "oneOf": [
        {
          "description": "Value track defined by explicit keyframes.",
          "required": [ "keys" ],
          "not": { "required": [ "path" ] }
        },
        {
          "description": "Value track defined by a path generator.",
          "required": [ "path" ],
          "not": { "required": [ "keys" ] }
        }
      ],
      "properties": {
        "parameter": {
          "description": "Catalog parameter key, fractal params key, or formula metadata key to animate.",
          "type": "string"
        },
        "keys": {
          "description": "Exactly two keyframes defining the animated value segment.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "$ref": "#/$defs/value-keyframe"
          }
        },
        "path": {
          "description": "Path generator that produces full-range keyframes for this track.",
          "$ref": "#/$defs/path-generator"
        }
      }
    },
    "camera2d-track": {
      "description": "Virtual 2D camera track that writes one catalog-declared viewport output.",
      "type": "object",
      "required": [ "name", "type", "output", "aspect", "look-at", "height" ],
      "additionalProperties": false,
      "anyOf": [
        {
          "description": "Camera orientation is provided directly by a view-up vector.",
          "required": [ "view-up" ]
        },
        {
          "description": "Camera orientation is derived from eye minus look-at.",
          "required": [ "eye" ]
        }
      ],
      "properties": {
        "name": {
          "description": "Local name used to report camera2d validation errors.",
          "type": "string"
        },
        "type": {
          "const": "camera2d",
          "description": "Selects virtual 2D camera evaluation."
        },
        "output": {
          "description": "Catalog parameter key that receives the computed viewport value.",
          "type": "string"
        },
        "aspect": {
          "const": "source",
          "description": "Use the source viewport aspect ratio."
        },
        "look-at": {
          "description": "Keyed point at the center of the planned camera view.",
          "$ref": "#/$defs/camera2d-point2-track"
        },
        "view-up": {
          "description": "Keyed up direction for the planned camera view.",
          "$ref": "#/$defs/camera2d-vector2-track"
        },
        "eye": {
          "description": "Point used with look-at to derive camera orientation; eye distance does not affect zoom.",
          "$ref": "#/$defs/camera2d-point2-track"
        },
        "height": {
          "description": "Keyed camera view height in output coordinate units.",
          "$ref": "#/$defs/camera2d-height-track"
        }
      }
    },
    "camera2d-point2-track": {
      "description": "Camera2d point2 component track.",
      "type": "object",
      "required": [ "type" ],
      "additionalProperties": false,
      "oneOf": [
        {
          "description": "Camera point is defined by two keyframes.",
          "required": [ "keys" ],
          "not": { "required": [ "path" ] }
        },
        {
          "description": "Camera point is generated by a full-frame path.",
          "required": [ "path" ],
          "not": { "required": [ "keys" ] }
        }
      ],
      "properties": {
        "type": {
          "const": "point2",
          "description": "Declares a two-component point value."
        },
        "keys": {
          "description": "Exactly two keyed point2 values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/camera2d-point2-keyframe" }
        },
        "path": {
          "description": "Path generator that produces the point value for each frame.",
          "$ref": "#/$defs/path-generator"
        }
      }
    },
    "camera2d-vector2-track": {
      "description": "Camera2d vector2 component track.",
      "type": "object",
      "required": [ "type", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "vector2",
          "description": "Declares a two-component direction value."
        },
        "normalize": {
          "const": true,
          "description": "Requests unit-vector normalization for this direction."
        },
        "keys": {
          "description": "Exactly two keyed vector2 values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/camera2d-vector2-keyframe" }
        }
      }
    },
    "camera2d-height-track": {
      "description": "Camera2d scalar height component track.",
      "type": "object",
      "required": [ "type", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "double",
          "description": "Declares a scalar floating-point value."
        },
        "keys": {
          "description": "Exactly two keyed positive height values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/camera2d-height-keyframe" }
        }
      }
    },
    "camera2d-point2-keyframe": {
      "description": "One keyed camera point2 value.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this camera point key.",
          "type": "integer"
        },
        "value": {
          "description": "Slash-delimited point2 value at this keyframe.",
          "type": "string"
        },
        "curve": {
          "description": "Optional interpolation curve ending at this point key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "camera2d-vector2-keyframe": {
      "description": "One keyed camera vector2 value.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this camera vector key.",
          "type": "integer"
        },
        "value": {
          "description": "Slash-delimited vector2 value at this keyframe.",
          "type": "string"
        },
        "curve": {
          "description": "Optional interpolation curve ending at this vector key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "camera2d-height-keyframe": {
      "description": "One keyed camera height value.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this camera height key.",
          "type": "integer"
        },
        "value": {
          "description": "Positive camera height at this keyframe.",
          "type": "number",
          "exclusiveMinimum": 0
        },
        "curve": {
          "description": "Optional interpolation curve ending at this height key.",
          "$ref": "#/$defs/curve"
        }
      }
    },
    "id-3d-view-track": {
      "description": "Virtual Id 3D view track that writes catalog-declared Euler-style output parameters.",
      "type": "object",
      "required": [ "name", "type", "outputs" ],
      "additionalProperties": false,
      "anyOf": [
        {
          "description": "Adapter includes a rotation component track.",
          "required": [ "rotation" ]
        },
        {
          "description": "Adapter includes a perspective component track.",
          "required": [ "perspective" ]
        },
        {
          "description": "Adapter includes an xyshift component track.",
          "required": [ "xyshift" ]
        },
        {
          "description": "Adapter includes a scalexyz component track.",
          "required": [ "scalexyz" ]
        },
        {
          "description": "Adapter includes a roughness component track.",
          "required": [ "roughness" ]
        },
        {
          "description": "Adapter includes a sphere component track.",
          "required": [ "sphere" ]
        },
        {
          "description": "Adapter includes a longitude component track.",
          "required": [ "longitude" ]
        },
        {
          "description": "Adapter includes a latitude component track.",
          "required": [ "latitude" ]
        },
        {
          "description": "Adapter includes a radius component track.",
          "required": [ "radius" ]
        },
        {
          "description": "Adapter includes a stereo component track.",
          "required": [ "stereo" ]
        },
        {
          "description": "Adapter includes an interocular component track.",
          "required": [ "interocular" ]
        },
        {
          "description": "Adapter includes a converge component track.",
          "required": [ "converge" ]
        }
      ],
      "properties": {
        "name": {
          "description": "Local name used to report id-3d-view validation errors.",
          "type": "string"
        },
        "type": {
          "const": "id-3d-view",
          "description": "Selects virtual Id 3D view evaluation."
        },
        "outputs": {
          "description": "Catalog parameter keys that receive computed 3D view values.",
          "$ref": "#/$defs/id-3d-view-outputs"
        },
        "rotation": {
          "description": "Keyed x/y/z rotation values.",
          "$ref": "#/$defs/id-3d-view-rotation-track"
        },
        "perspective": {
          "description": "Keyed perspective values.",
          "$ref": "#/$defs/id-3d-view-perspective-track"
        },
        "xyshift": {
          "description": "Keyed x/y image shift values.",
          "$ref": "#/$defs/id-3d-view-xyshift-track"
        },
        "scalexyz": {
          "description": "Keyed x/y/z scale factors.",
          "$ref": "#/$defs/id-3d-view-scalexyz-track"
        },
        "roughness": {
          "description": "Keyed z scale factor alias values.",
          "$ref": "#/$defs/id-3d-view-integer-track"
        },
        "sphere": {
          "description": "Keyed sphere projection flag values.",
          "$ref": "#/$defs/id-3d-view-sphere-track"
        },
        "longitude": {
          "description": "Keyed longitude minimum and maximum values.",
          "$ref": "#/$defs/id-3d-view-longitude-track"
        },
        "latitude": {
          "description": "Keyed latitude minimum and maximum values.",
          "$ref": "#/$defs/id-3d-view-latitude-track"
        },
        "radius": {
          "description": "Keyed sphere radius scale values.",
          "$ref": "#/$defs/id-3d-view-integer-track"
        },
        "stereo": {
          "description": "Keyed stereo mode values.",
          "$ref": "#/$defs/id-3d-view-stereo-track"
        },
        "interocular": {
          "description": "Keyed stereo eye separation values.",
          "$ref": "#/$defs/id-3d-view-integer-track"
        },
        "converge": {
          "description": "Keyed stereo convergence values.",
          "$ref": "#/$defs/id-3d-view-integer-track"
        }
      }
    },
    "id-3d-view-outputs": {
      "description": "Output parameter names for the Id 3D view adapter.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": false,
      "properties": {
        "rotation": {
          "description": "Catalog key that receives the rotation tuple.",
          "type": "string"
        },
        "perspective": {
          "description": "Catalog key that receives the perspective integer.",
          "type": "string"
        },
        "xyshift": {
          "description": "Catalog key that receives the xyshift tuple.",
          "type": "string"
        },
        "scalexyz": {
          "description": "Catalog key that receives the scalexyz tuple.",
          "type": "string"
        },
        "roughness": {
          "description": "Catalog key that receives the roughness integer.",
          "type": "string"
        },
        "sphere": {
          "description": "Catalog key that receives the sphere enum value.",
          "type": "string"
        },
        "longitude": {
          "description": "Catalog key that receives the longitude tuple.",
          "type": "string"
        },
        "latitude": {
          "description": "Catalog key that receives the latitude tuple.",
          "type": "string"
        },
        "radius": {
          "description": "Catalog key that receives the radius integer.",
          "type": "string"
        },
        "stereo": {
          "description": "Catalog key that receives the stereo integer.",
          "type": "string"
        },
        "interocular": {
          "description": "Catalog key that receives the interocular integer.",
          "type": "string"
        },
        "converge": {
          "description": "Catalog key that receives the converge integer.",
          "type": "string"
        }
      }
    },
    "id-3d-view-rotation-track": {
      "description": "Id 3D rotation component track.",
      "type": "object",
      "required": [ "type", "arity", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "numeric-tuple",
          "description": "Declares a slash-delimited numeric tuple value."
        },
        "arity": {
          "const": 3,
          "description": "Rotation uses three components: x, y, and z."
        },
        "keys": {
          "description": "Exactly two keyed rotation tuple values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/id-3d-view-tuple-keyframe" }
        }
      }
    },
    "id-3d-view-perspective-track": {
      "description": "Id 3D perspective component track.",
      "type": "object",
      "required": [ "type", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "integer",
          "description": "Declares an integer value."
        },
        "keys": {
          "description": "Exactly two keyed perspective integer values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/id-3d-view-integer-keyframe" }
        }
      }
    },
    "id-3d-view-xyshift-track": {
      "description": "Id 3D xyshift component track.",
      "type": "object",
      "required": [ "type", "arity", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "numeric-tuple",
          "description": "Declares a slash-delimited numeric tuple value."
        },
        "arity": {
          "const": 2,
          "description": "Xyshift uses two components: x and y."
        },
        "keys": {
          "description": "Exactly two keyed xyshift tuple values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/id-3d-view-tuple-keyframe" }
        }
      }
    },
    "id-3d-view-scalexyz-track": {
      "description": "Id 3D scalexyz component track.",
      "type": "object",
      "required": [ "type", "arity", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "numeric-tuple",
          "description": "Declares a slash-delimited numeric tuple value."
        },
        "arity": {
          "const": 3,
          "description": "Scalexyz uses three components: x, y, and z."
        },
        "keys": {
          "description": "Exactly two keyed scalexyz tuple values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/id-3d-view-tuple-keyframe" }
        }
      }
    },
    "id-3d-view-longitude-track": {
      "description": "Id 3D longitude component track.",
      "type": "object",
      "required": [ "type", "arity", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "numeric-tuple",
          "description": "Declares a slash-delimited numeric tuple value."
        },
        "arity": {
          "const": 2,
          "description": "Longitude uses two components: minimum and maximum."
        },
        "keys": {
          "description": "Exactly two keyed longitude tuple values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/id-3d-view-tuple-keyframe" }
        }
      }
    },
    "id-3d-view-latitude-track": {
      "description": "Id 3D latitude component track.",
      "type": "object",
      "required": [ "type", "arity", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "numeric-tuple",
          "description": "Declares a slash-delimited numeric tuple value."
        },
        "arity": {
          "const": 2,
          "description": "Latitude uses two components: minimum and maximum."
        },
        "keys": {
          "description": "Exactly two keyed latitude tuple values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/id-3d-view-tuple-keyframe" }
        }
      }
    },
    "id-3d-view-integer-track": {
      "description": "Id 3D integer component track.",
      "type": "object",
      "required": [ "type", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "integer",
          "description": "Declares an integer value."
        },
        "keys": {
          "description": "Exactly two keyed integer values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/id-3d-view-integer-keyframe" }
        }
      }
    },
    "id-3d-view-stereo-track": {
      "description": "Id 3D stereo mode component track.",
      "type": "object",
      "required": [ "type", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "integer",
          "description": "Declares an integer stereo mode value."
        },
        "keys": {
          "description": "Exactly two keyed stereo mode values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/id-3d-view-stereo-keyframe" }
        }
      }
    },
    "id-3d-view-sphere-track": {
      "description": "Id 3D sphere projection component track.",
      "type": "object",
      "required": [ "type", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "enum",
          "description": "Declares an enum value."
        },
        "keys": {
          "description": "Exactly two keyed sphere flag values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/id-3d-view-sphere-keyframe" }
        }
      }
    },
    "id-3d-view-tuple-keyframe": {
      "description": "One keyed Id 3D tuple value.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this tuple key.",
          "type": "integer"
        },
        "value": {
          "description": "Slash-delimited tuple value at this keyframe.",
          "type": "string"
        },
        "curve": {
          "description": "Optional interpolation curve ending at this tuple key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "id-3d-view-integer-keyframe": {
      "description": "One keyed Id 3D integer value.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this integer key.",
          "type": "integer"
        },
        "value": {
          "description": "Integer value at this keyframe.",
          "type": "integer"
        },
        "curve": {
          "description": "Optional interpolation curve ending at this integer key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "id-3d-view-stereo-keyframe": {
      "description": "One keyed Id 3D stereo mode value.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this stereo key.",
          "type": "integer"
        },
        "value": {
          "description": "Stereo mode: 0 none, 1 alternate, 2 superimpose, 3 photo, or 4 stereo pair.",
          "type": "integer",
          "minimum": 0,
          "maximum": 4
        },
        "curve": {
          "description": "Optional interpolation curve ending at this stereo key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "id-3d-view-sphere-keyframe": {
      "description": "One keyed Id 3D sphere flag value.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this sphere key.",
          "type": "integer"
        },
        "value": {
          "description": "Sphere projection flag value accepted by Id.",
          "type": "string",
          "oneOf": [
            {
              "const": "yes",
              "description": "Enable sphere projection."
            },
            {
              "const": "no",
              "description": "Disable sphere projection."
            },
            {
              "const": "y",
              "description": "Short form that enables sphere projection."
            },
            {
              "const": "n",
              "description": "Short form that disables sphere projection."
            }
          ]
        },
        "curve": {
          "description": "Optional interpolation curve ending at this sphere key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "julibrot-view-track": {
      "description": "Virtual Julibrot view track that writes catalog-declared slice and stereo output parameters.",
      "type": "object",
      "required": [ "name", "type", "outputs" ],
      "additionalProperties": false,
      "anyOf": [
        {
          "description": "Adapter includes a 3dmode component track.",
          "required": [ "mode" ]
        },
        {
          "description": "Adapter includes a julibrot3d component track.",
          "required": [ "geometry" ]
        },
        {
          "description": "Adapter includes a julibroteyes component track.",
          "required": [ "eyes" ]
        },
        {
          "description": "Adapter includes a julibrotfromto component track.",
          "required": [ "from-to" ]
        }
      ],
      "properties": {
        "name": {
          "description": "Local name used to report julibrot-view validation errors.",
          "type": "string"
        },
        "type": {
          "const": "julibrot-view",
          "description": "Selects virtual Julibrot view evaluation."
        },
        "outputs": {
          "description": "Catalog parameter keys that receive computed Julibrot view values.",
          "$ref": "#/$defs/julibrot-view-outputs"
        },
        "mode": {
          "description": "Keyed Julibrot 3D mode values.",
          "$ref": "#/$defs/julibrot-view-mode-track"
        },
        "geometry": {
          "description": "Keyed Julibrot z dots, origin, depth, height, width, and distance values.",
          "$ref": "#/$defs/julibrot-view-geometry-track"
        },
        "eyes": {
          "description": "Keyed Julibrot virtual eye separation values.",
          "$ref": "#/$defs/julibrot-view-eyes-track"
        },
        "from-to": {
          "description": "Keyed Julibrot slice from/to coordinate values.",
          "$ref": "#/$defs/julibrot-view-from-to-track"
        }
      }
    },
    "julibrot-view-outputs": {
      "description": "Output parameter names for the Julibrot view adapter.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": false,
      "properties": {
        "mode": {
          "description": "Catalog key that receives the 3dmode enum value.",
          "type": "string"
        },
        "geometry": {
          "description": "Catalog key that receives the julibrot3d tuple.",
          "type": "string"
        },
        "eyes": {
          "description": "Catalog key that receives the julibroteyes scalar.",
          "type": "string"
        },
        "from-to": {
          "description": "Catalog key that receives the julibrotfromto tuple.",
          "type": "string"
        }
      }
    },
    "julibrot-view-mode-track": {
      "description": "Julibrot 3D mode component track.",
      "type": "object",
      "required": [ "type", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "enum",
          "description": "Declares an enum value."
        },
        "keys": {
          "description": "Exactly two keyed Julibrot mode values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/julibrot-view-mode-keyframe" }
        }
      }
    },
    "julibrot-view-geometry-track": {
      "description": "Julibrot geometry component track.",
      "type": "object",
      "required": [ "type", "arity", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "numeric-tuple",
          "description": "Declares a slash-delimited numeric tuple value."
        },
        "arity": {
          "const": 6,
          "description": "Julibrot geometry uses z dots, origin, depth, height, width, and distance."
        },
        "keys": {
          "description": "Exactly two keyed Julibrot geometry tuple values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/julibrot-view-tuple-keyframe" }
        }
      }
    },
    "julibrot-view-eyes-track": {
      "description": "Julibrot virtual eye separation component track.",
      "type": "object",
      "required": [ "type", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "double",
          "description": "Declares a scalar floating-point value."
        },
        "keys": {
          "description": "Exactly two keyed Julibrot eye separation values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/julibrot-view-double-keyframe" }
        }
      }
    },
    "julibrot-view-from-to-track": {
      "description": "Julibrot from-to slice component track.",
      "type": "object",
      "required": [ "type", "arity", "keys" ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "numeric-tuple",
          "description": "Declares a slash-delimited numeric tuple value."
        },
        "arity": {
          "const": 4,
          "description": "Julibrot from-to uses x max, x min, y max, and y min."
        },
        "keys": {
          "description": "Exactly two keyed Julibrot from-to tuple values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "$ref": "#/$defs/julibrot-view-tuple-keyframe" }
        }
      }
    },
    "julibrot-view-mode-keyframe": {
      "description": "One keyed Julibrot 3D mode value.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this Julibrot mode key.",
          "type": "integer"
        },
        "value": {
          "description": "Julibrot 3D mode value accepted by Id.",
          "type": "string",
          "oneOf": [
            {
              "const": "monocular",
              "description": "Render a single centered Julibrot view."
            },
            {
              "const": "lefteye",
              "description": "Render the left-eye Julibrot view."
            },
            {
              "const": "righteye",
              "description": "Render the right-eye Julibrot view."
            },
            {
              "const": "red-blue",
              "description": "Render Julibrot red-blue stereo output."
            }
          ]
        },
        "curve": {
          "description": "Optional interpolation curve ending at this Julibrot mode key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "julibrot-view-tuple-keyframe": {
      "description": "One keyed Julibrot tuple value.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this Julibrot tuple key.",
          "type": "integer"
        },
        "value": {
          "description": "Slash-delimited Julibrot tuple value at this keyframe.",
          "type": "string"
        },
        "curve": {
          "description": "Optional interpolation curve ending at this Julibrot tuple key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "julibrot-view-double-keyframe": {
      "description": "One keyed Julibrot scalar value.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this Julibrot scalar key.",
          "type": "integer"
        },
        "value": {
          "description": "Floating-point Julibrot value at this keyframe.",
          "type": "number"
        },
        "curve": {
          "description": "Optional interpolation curve ending at this Julibrot scalar key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "path-generator": {
      "description": "Full-range value generator used instead of explicit keyframes.",
      "oneOf": [
        { "$ref": "#/$defs/constant-path-generator" },
        { "$ref": "#/$defs/line-path-generator" },
        { "$ref": "#/$defs/circle-path-generator" },
        { "$ref": "#/$defs/ellipse-path-generator" },
        { "$ref": "#/$defs/lissajous-path-generator" },
        { "$ref": "#/$defs/spiral-path-generator" },
        { "$ref": "#/$defs/bezier-path-generator" },
        { "$ref": "#/$defs/catmull-rom-path-generator" }
      ]
    },
    "constant-path-generator": {
      "description": "Path generator that emits one fixed value for every frame.",
      "type": "object",
      "required": [ "kind", "value" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "constant",
          "description": "Generate a full-range constant value path."
        },
        "value": {
          "description": "Parameter value text emitted for every frame.",
          "type": "string"
        }
      }
    },
    "line-path-generator": {
      "description": "Path generator that linearly interpolates from one value to another.",
      "type": "object",
      "required": [ "kind", "from", "to" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "line",
          "description": "Generate a full-range linear value path."
        },
        "from": {
          "description": "Parameter value text at frame zero.",
          "type": "string"
        },
        "to": {
          "description": "Parameter value text at the final frame.",
          "type": "string"
        }
      }
    },
    "circle-path-generator": {
      "description": "Path generator that samples a circle over the full frame range.",
      "type": "object",
      "required": [ "kind", "center", "radius" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "circle",
          "description": "Generate values on a circular path."
        },
        "center": {
          "description": "Path center as a slash-pair value.",
          "type": "string"
        },
        "radius": {
          "description": "Circle radius in output value units.",
          "type": "number",
          "minimum": 0
        },
        "turns": {
          "description": "Number of complete turns sampled over the full frame range.",
          "type": "number"
        },
        "phase": {
          "description": "Starting phase angle in degrees.",
          "type": "number"
        }
      }
    },
    "ellipse-path-generator": {
      "description": "Path generator that samples an axis-aligned ellipse over the full frame range.",
      "type": "object",
      "required": [ "kind", "center", "x-radius", "y-radius" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "ellipse",
          "description": "Generate values on an axis-aligned elliptical path."
        },
        "center": {
          "description": "Path center as a slash-pair value.",
          "type": "string"
        },
        "x-radius": {
          "description": "Ellipse radius along the first output component.",
          "type": "number",
          "minimum": 0
        },
        "y-radius": {
          "description": "Ellipse radius along the second output component.",
          "type": "number",
          "minimum": 0
        },
        "turns": {
          "description": "Number of complete turns sampled over the full frame range.",
          "type": "number"
        },
        "phase": {
          "description": "Starting phase angle in degrees.",
          "type": "number"
        }
      }
    },
    "lissajous-path-generator": {
      "description": "Path generator that samples a Lissajous curve over the full frame range.",
      "type": "object",
      "required": [ "kind", "center", "x-radius", "y-radius", "x-frequency", "y-frequency" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "lissajous",
          "description": "Generate values on a Lissajous curve."
        },
        "center": {
          "description": "Path center as a slash-pair value.",
          "type": "string"
        },
        "x-radius": {
          "description": "Lissajous radius along the first output component.",
          "type": "number",
          "minimum": 0
        },
        "y-radius": {
          "description": "Lissajous radius along the second output component.",
          "type": "number",
          "minimum": 0
        },
        "x-frequency": {
          "description": "Positive frequency multiplier for the first output component.",
          "type": "number",
          "exclusiveMinimum": 0
        },
        "y-frequency": {
          "description": "Positive frequency multiplier for the second output component.",
          "type": "number",
          "exclusiveMinimum": 0
        },
        "phase": {
          "description": "Starting phase angle in degrees for the first output component.",
          "type": "number"
        }
      }
    },
    "spiral-path-generator": {
      "description": "Path generator that samples a radial spiral over the full frame range.",
      "type": "object",
      "required": [ "kind", "center", "from-radius", "to-radius" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "spiral",
          "description": "Generate values on a spiral path."
        },
        "center": {
          "description": "Path center as a slash-pair value.",
          "type": "string"
        },
        "from-radius": {
          "description": "Spiral radius at frame zero.",
          "type": "number",
          "minimum": 0
        },
        "to-radius": {
          "description": "Spiral radius at the final frame.",
          "type": "number",
          "minimum": 0
        },
        "turns": {
          "description": "Number of complete turns sampled over the full frame range.",
          "type": "number"
        },
        "phase": {
          "description": "Starting phase angle in degrees.",
          "type": "number"
        }
      }
    },
    "bezier-path-generator": {
      "description": "Path generator that samples a Bezier curve over the full frame range.",
      "type": "object",
      "required": [ "kind", "control-points" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "bezier",
          "description": "Generate values on a Bezier curve."
        },
        "control-points": {
          "description": "Slash-delimited numeric control points for the Bezier curve.",
          "type": "array",
          "minItems": 2,
          "items": {
            "description": "One slash-delimited numeric Bezier control point.",
            "type": "string"
          }
        }
      }
    },
    "catmull-rom-path-generator": {
      "description": "Path generator that samples a Catmull-Rom spline over the full frame range.",
      "type": "object",
      "required": [ "kind", "control-points" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "catmull-rom",
          "description": "Generate values on a Catmull-Rom spline."
        },
        "control-points": {
          "description": "Slash-delimited numeric control points for the Catmull-Rom spline.",
          "type": "array",
          "minItems": 4,
          "items": {
            "description": "One slash-delimited numeric Catmull-Rom control point.",
            "type": "string"
          }
        }
      }
    },
    "value-keyframe": {
      "description": "One keyed parameter value at an animation frame.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this key.",
          "type": "integer"
        },
        "value": {
          "description": "Parameter value text in the format required by the target metadata.",
          "type": "string"
        },
        "curve": {
          "description": "Optional interpolation curve override ending at this key.",
          "$ref": "#/$defs/curve"
        }
      }
    },
    "pwm-track": {
      "description": "Discrete track that temporally dithers between two explicit values.",
      "type": "object",
      "required": [ "parameter", "mode", "a", "b", "window", "keys" ],
      "additionalProperties": false,
      "properties": {
        "parameter": {
          "description": "Catalog parameter key, fractal params key, or formula metadata key to animate.",
          "type": "string"
        },
        "mode": {
          "description": "Selects pulse-width modulation for this track.",
          "$ref": "#/$defs/track-mode"
        },
        "a": {
          "description": "Discrete value emitted when the evaluated mix selects the first endpoint.",
          "type": "string"
        },
        "b": {
          "description": "Discrete value emitted when the evaluated mix selects the second endpoint.",
          "type": "string"
        },
        "window": {
          "description": "Number of frames in each PWM decision window.",
          "type": "integer",
          "minimum": 2
        },
        "keys": {
          "description": "Exactly two keyframes defining the animated mix segment.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "$ref": "#/$defs/pwm-keyframe"
          }
        }
      }
    },
    "pwm-keyframe": {
      "description": "One keyed PWM mix value at an animation frame.",
      "type": "object",
      "required": [ "frame", "mix" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this key.",
          "type": "integer"
        },
        "mix": {
          "description": "Fraction of the PWM window assigned to value b.",
          "type": "number",
          "minimum": 0,
          "maximum": 1
        }
      }
    },
    "color-map-track": {
      "description": "Track that writes generated frame map files using keyed maps, source maps, or generated sources.",
      "type": "object",
      "required": [ "parameter", "type", "format", "output" ],
      "additionalProperties": false,
      "oneOf": [
        {
          "description": "Color-map shorthand that interpolates between keyed source map files.",
          "required": [ "keys" ],
          "not": {
            "anyOf": [
              { "required": [ "source" ] },
              { "required": [ "effects" ] }
            ]
          }
        },
        {
          "description": "Color-map track that starts from one file or generated source.",
          "required": [ "source" ],
          "not": { "required": [ "keys" ] }
        }
      ],
      "properties": {
        "parameter": {
          "description": "Id parameter to receive the generated at-file map reference.",
          "type": "string"
        },
        "type": {
          "description": "Selects color-map side-file generation for this track.",
          "const": "color-map"
        },
        "format": {
          "description": "Format for the generated Id parameter value.",
          "$ref": "#/$defs/track-format"
        },
        "output": {
          "description": "Generated map filename pattern written under the output map directory.",
          "type": "string"
        },
        "keys": {
          "description": "Exactly two keyframes selecting source map files.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "$ref": "#/$defs/value-keyframe"
          }
        },
        "source": {
          "description": "Source Id map file or generated source read before applying ordered effects.",
          "$ref": "#/$defs/color-map-source"
        },
        "effects": {
          "description": "Ordered color-map effects applied to the source map.",
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/color-map-effect"
          }
        }
      }
    },
    "color-map-source": {
      "description": "Source used to produce a frame color map before effects.",
      "oneOf": [
        {
          "description": "Source Id map file read from disk.",
          "type": "string"
        },
        {
          "description": "Generated gradient source object.",
          "$ref": "#/$defs/gradient-color-map-source"
        }
      ]
    },
    "gradient-color-map-source": {
      "description": "Generated color map made from indexed color stops.",
      "type": "object",
      "required": [ "kind", "stops" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "gradient",
          "description": "Generate a color map by interpolating indexed color stops."
        },
        "stops": {
          "description": "Color stops sorted by palette index.",
          "type": "array",
          "minItems": 2,
          "items": {
            "description": "One indexed color stop.",
            "$ref": "#/$defs/gradient-color-map-stop"
          }
        }
      }
    },
    "gradient-color-map-stop": {
      "description": "One indexed color stop in a generated gradient map.",
      "type": "object",
      "required": [ "index", "color" ],
      "additionalProperties": false,
      "properties": {
        "index": {
          "description": "Palette index for this color stop.",
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "color": {
          "description": "Color specification used at this palette index.",
          "$ref": "#/$defs/color-specification"
        }
      }
    },
    "color-specification": {
      "description": "Concrete color written as a CSS named color, RGB tuple, HSV tuple, or HSL tuple.",
      "type": "string",
      "oneOf": [
        {
          "description": "CSS named color, case-insensitive; accepted names are checked against the W3C CSS Color Module Level 4 named-colors table.",
          "pattern": "^[A-Za-z]+$"
        },
        {
          "description": "RGB slash tuple, with optional rgb: prefix and integer components from 0 through 255.",
          "pattern": "^(?:rgb:)?(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9]?[0-9])/(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9]?[0-9])/(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9]?[0-9])$"
        },
        {
          "description": "HSV slash tuple with hue from 0 through 360 and saturation/value from 0 through 1.",
          "pattern": "^hsv:(?:360(?:\\.0*)?|3[0-5][0-9](?:\\.[0-9]*)?|[12][0-9][0-9](?:\\.[0-9]*)?|[1-9]?[0-9](?:\\.[0-9]*)?)/(?:1(?:\\.0*)?|0(?:\\.[0-9]*)?)/(?:1(?:\\.0*)?|0(?:\\.[0-9]*)?)$"
        },
        {
          "description": "HSL slash tuple with hue from 0 through 360 and saturation/lightness from 0 through 1.",
          "pattern": "^hsl:(?:360(?:\\.0*)?|3[0-5][0-9](?:\\.[0-9]*)?|[12][0-9][0-9](?:\\.[0-9]*)?|[1-9]?[0-9](?:\\.[0-9]*)?)/(?:1(?:\\.0*)?|0(?:\\.[0-9]*)?)/(?:1(?:\\.0*)?|0(?:\\.[0-9]*)?)$"
        }
      ]
    },
    "color-map-effect": {
      "description": "One transformation applied to a color map.",
      "oneOf": [
        { "$ref": "#/$defs/brightness-color-map-effect" },
        { "$ref": "#/$defs/contrast-color-map-effect" },
        { "$ref": "#/$defs/gamma-color-map-effect" },
        { "$ref": "#/$defs/hue-shift-color-map-effect" },
        { "$ref": "#/$defs/mask-blend-color-map-effect" },
        { "$ref": "#/$defs/pulse-color-map-effect" },
        { "$ref": "#/$defs/remap-color-map-effect" },
        { "$ref": "#/$defs/reverse-color-map-effect" },
        { "$ref": "#/$defs/saturation-color-map-effect" },
        { "$ref": "#/$defs/sparkle-color-map-effect" },
        { "$ref": "#/$defs/ping-pong-color-map-effect" }
      ]
    },
    "brightness-color-map-effect": {
      "description": "Effect that scales every RGB component by a keyed amount.",
      "type": "object",
      "required": [ "kind", "amount" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "brightness",
          "description": "Scale RGB components by a keyed amount."
        },
        "amount": {
          "description": "Keyed brightness multiplier.",
          "$ref": "#/$defs/number-track"
        }
      }
    },
    "contrast-color-map-effect": {
      "description": "Effect that scales RGB component distance from midgray.",
      "type": "object",
      "required": [ "kind", "amount" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "contrast",
          "description": "Scale RGB component distance from midgray."
        },
        "amount": {
          "description": "Keyed contrast multiplier.",
          "$ref": "#/$defs/number-track"
        }
      }
    },
    "gamma-color-map-effect": {
      "description": "Effect that applies nonlinear intensity shaping.",
      "type": "object",
      "required": [ "kind", "amount" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "gamma",
          "description": "Apply nonlinear intensity shaping."
        },
        "amount": {
          "description": "Keyed positive gamma exponent.",
          "$ref": "#/$defs/positive-number-track"
        }
      }
    },
    "hue-shift-color-map-effect": {
      "description": "Effect that rotates color hue in HSL space.",
      "type": "object",
      "required": [ "kind", "amount" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "hue-shift",
          "description": "Rotate color hue by keyed degrees."
        },
        "amount": {
          "description": "Keyed hue rotation in degrees.",
          "$ref": "#/$defs/number-track"
        }
      }
    },
    "mask-blend-color-map-effect": {
      "description": "Effect that blends selected ranges with another Id map file.",
      "type": "object",
      "required": [ "kind", "ranges", "source", "amount" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "mask-blend",
          "description": "Blend selected ranges with another Id map file."
        },
        "ranges": {
          "description": "Inclusive palette index ranges to blend.",
          "$ref": "#/$defs/color-map-ranges"
        },
        "source": {
          "description": "Source Id map file used for blended entries.",
          "type": "string"
        },
        "amount": {
          "description": "Keyed blend amount from 0 through 1.",
          "$ref": "#/$defs/unit-number-track"
        }
      }
    },
    "pulse-color-map-effect": {
      "description": "Effect that blends one range toward a concrete color.",
      "type": "object",
      "required": [ "kind", "range", "color", "amount" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "pulse",
          "description": "Blend one range toward a concrete color."
        },
        "range": {
          "description": "Inclusive palette index range to pulse.",
          "$ref": "#/$defs/color-map-range"
        },
        "color": {
          "description": "Concrete pulse color.",
          "$ref": "#/$defs/color-specification"
        },
        "amount": {
          "description": "Keyed pulse amount from 0 through 1.",
          "$ref": "#/$defs/unit-number-track"
        }
      }
    },
    "remap-color-map-effect": {
      "description": "Effect that reindexes the map with a 256-entry lookup table.",
      "type": "object",
      "required": [ "kind", "indices" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "remap",
          "description": "Reindex the map with a 256-entry lookup table."
        },
        "indices": {
          "description": "Destination-indexed source palette index table.",
          "$ref": "#/$defs/color-map-remap-indices"
        }
      }
    },
    "reverse-color-map-effect": {
      "description": "Effect that reverses a full color map or one inclusive range.",
      "type": "object",
      "required": [ "kind" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "reverse",
          "description": "Reverse the selected color-map entries."
        },
        "range": {
          "description": "Optional inclusive palette index range to reverse.",
          "$ref": "#/$defs/color-map-range"
        }
      }
    },
    "saturation-color-map-effect": {
      "description": "Effect that scales color saturation in HSL space.",
      "type": "object",
      "required": [ "kind", "amount" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "saturation",
          "description": "Scale color saturation by a keyed amount."
        },
        "amount": {
          "description": "Keyed saturation multiplier.",
          "$ref": "#/$defs/number-track"
        }
      }
    },
    "sparkle-color-map-effect": {
      "description": "Effect that applies seeded random color perturbation in one range.",
      "type": "object",
      "required": [ "kind", "range", "seed", "amount" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "sparkle",
          "description": "Apply seeded random color perturbation in one range."
        },
        "range": {
          "description": "Inclusive palette index range to perturb.",
          "$ref": "#/$defs/color-map-range"
        },
        "seed": {
          "description": "Integer seed for repeatable random perturbation.",
          "type": "integer",
          "minimum": 0,
          "maximum": 2147483647
        },
        "amount": {
          "description": "Keyed maximum component delta from 0 through 255.",
          "$ref": "#/$defs/byte-number-track"
        }
      }
    },
    "ping-pong-color-map-effect": {
      "description": "Effect that rotates entries forward, then backward, over a ping-pong cycle.",
      "type": "object",
      "required": [ "kind", "offset" ],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "ping-pong",
          "description": "Apply a ping-pong palette offset."
        },
        "range": {
          "description": "Optional inclusive palette index range to ping-pong.",
          "$ref": "#/$defs/color-map-range"
        },
        "offset": {
          "description": "Keyed numeric offset sampled before ping-pong wrapping.",
          "$ref": "#/$defs/number-track"
        }
      }
    },
    "color-map-range": {
      "description": "Inclusive color-map palette index range.",
      "type": "array",
      "minItems": 2,
      "maxItems": 2,
      "items": {
        "description": "Selected palette index.",
        "type": "integer",
        "minimum": 0,
        "maximum": 255
      }
    },
    "color-map-ranges": {
      "description": "One or more inclusive color-map palette index ranges.",
      "type": "array",
      "minItems": 1,
      "items": {
        "description": "Inclusive color-map palette index range.",
        "$ref": "#/$defs/color-map-range"
      }
    },
    "color-map-remap-indices": {
      "description": "Destination-indexed table of source palette indices.",
      "type": "array",
      "minItems": 256,
      "maxItems": 256,
      "items": {
        "description": "Source palette index.",
        "type": "integer",
        "minimum": 0,
        "maximum": 255
      }
    },
    "unit-number-track": {
      "description": "Keyed numeric effect parameter from 0 through 1.",
      "type": "object",
      "required": [ "keys" ],
      "additionalProperties": false,
      "properties": {
        "keys": {
          "description": "Exactly two keyed numeric values from 0 through 1.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "$ref": "#/$defs/unit-number-keyframe"
          }
        }
      }
    },
    "unit-number-keyframe": {
      "description": "One keyed numeric value from 0 through 1.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this unit numeric key.",
          "type": "integer"
        },
        "value": {
          "description": "Numeric value sampled at this keyframe.",
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "curve": {
          "description": "Optional interpolation curve ending at this unit numeric key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "byte-number-track": {
      "description": "Keyed numeric effect parameter from 0 through 255.",
      "type": "object",
      "required": [ "keys" ],
      "additionalProperties": false,
      "properties": {
        "keys": {
          "description": "Exactly two keyed numeric values from 0 through 255.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "$ref": "#/$defs/byte-number-keyframe"
          }
        }
      }
    },
    "byte-number-keyframe": {
      "description": "One keyed numeric value from 0 through 255.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this byte numeric key.",
          "type": "integer"
        },
        "value": {
          "description": "Numeric value sampled at this keyframe.",
          "type": "number",
          "minimum": 0,
          "maximum": 255
        },
        "curve": {
          "description": "Optional interpolation curve ending at this byte numeric key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "positive-number-track": {
      "description": "Keyed positive numeric effect parameter.",
      "type": "object",
      "required": [ "keys" ],
      "additionalProperties": false,
      "properties": {
        "keys": {
          "description": "Exactly two keyed positive numeric values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "$ref": "#/$defs/positive-number-keyframe"
          }
        }
      }
    },
    "positive-number-keyframe": {
      "description": "One keyed positive numeric value at an animation frame.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this positive numeric key.",
          "type": "integer"
        },
        "value": {
          "description": "Positive numeric value sampled at this keyframe.",
          "type": "number",
          "exclusiveMinimum": 0
        },
        "curve": {
          "description": "Optional interpolation curve ending at this positive numeric key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "percent-number-track": {
      "description": "Keyed numeric percent from 0 through 100.",
      "type": "object",
      "required": [ "keys" ],
      "additionalProperties": false,
      "properties": {
        "keys": {
          "description": "Exactly two keyed percent values from 0 through 100.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "$ref": "#/$defs/percent-number-keyframe"
          }
        }
      }
    },
    "percent-number-keyframe": {
      "description": "One keyed numeric percent value at an animation frame.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this percent key.",
          "type": "integer"
        },
        "value": {
          "description": "Percent value sampled at this keyframe.",
          "type": "number",
          "minimum": 0,
          "maximum": 100
        },
        "curve": {
          "description": "Optional interpolation curve ending at this percent key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "number-track": {
      "description": "Keyed numeric effect parameter.",
      "type": "object",
      "required": [ "keys" ],
      "additionalProperties": false,
      "properties": {
        "keys": {
          "description": "Exactly two keyed numeric values.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "$ref": "#/$defs/number-keyframe"
          }
        }
      }
    },
    "number-keyframe": {
      "description": "One keyed numeric value at an animation frame.",
      "type": "object",
      "required": [ "frame", "value" ],
      "additionalProperties": false,
      "properties": {
        "frame": {
          "description": "Zero-based frame index for this numeric key.",
          "type": "integer"
        },
        "value": {
          "description": "Numeric value sampled at this keyframe.",
          "type": "number"
        },
        "curve": {
          "description": "Optional interpolation curve ending at this numeric key.",
          "$ref": "#/$defs/scalar-curve"
        }
      }
    },
    "scalar-curve": {
      "description": "Interpolation curve used for keyed scalar effect parameters.",
      "type": "string",
      "oneOf": [
        {
          "const": "hold",
          "description": "Hold the source value until the destination key."
        },
        {
          "const": "linear",
          "description": "Linearly interpolate numeric values."
        },
        {
          "const": "step",
          "description": "Step from the source value to the destination value at the destination key."
        }
      ]
    },
    "curve": {
      "description": "Interpolation curve used between two keyframes.",
      "type": "string",
      "oneOf": [
        {
          "const": "geometric",
          "description": "Geometric interpolation, currently used for magnification values."
        },
        {
          "const": "hold",
          "description": "Hold the source value until the destination key."
        },
        {
          "const": "linear",
          "description": "Linearly interpolate numeric values."
        },
        {
          "const": "step",
          "description": "Step from the source value to the destination value at the destination key."
        }
      ]
    }
  }
}
