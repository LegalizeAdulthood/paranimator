{
  "parameters": {},
  "formula-entries": {
    "DAFrm01": {
      "functions": {
        "fn1": {
          "type": "enum",
          "values": "id-functions",
          "description": "Selects fn1 added to pixel before multiplying by z^(z-1).",
          "format": "raw",
          "default-curve": "hold",
          "extrapolate": "clamp"
        }
      }
    },
    "DAFrm07": {
      "functions": {
        "fn1": {
          "type": "enum",
          "values": "id-functions",
          "description": "Selects fn1 multiplied by z^(z-1) before adding pixel.",
          "format": "raw",
          "default-curve": "hold",
          "extrapolate": "clamp"
        }
      }
    },
    "Larry": {
      "params": {
        "knobs": {
          "fractal parameter": {
            "type": "complex",
            "variable": "p1",
            "description": "Sets Larry's complex c parameter; 0/0 uses the default 0.5.",
            "format": "slash-pair",
            "default-curve": "linear",
            "extrapolate": "clamp"
          },
          "bailout": {
            "type": "real",
            "variable": "p2.real",
            "description": "Sets Larry's bailout threshold; nonpositive values use 4.",
            "format": "raw",
            "default-curve": "linear",
            "extrapolate": "clamp"
          }
        }
      },
      "functions": {
        "fn1": {
          "type": "enum",
          "values": "id-functions",
          "description": "Selects outer fn1 in z=fn1(fn2(z*z))+c.",
          "format": "raw",
          "default-curve": "hold",
          "extrapolate": "clamp"
        },
        "fn2": {
          "type": "enum",
          "values": "id-functions",
          "description": "Selects inner fn2 in z=fn1(fn2(z*z))+c.",
          "format": "raw",
          "default-curve": "hold",
          "extrapolate": "clamp"
        }
      }
    }
  }
}
